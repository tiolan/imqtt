mqttlib:
  default: mosquitto
  choices:
    mosquitto:
      short: mosq
      settings:
        USE_MOSQ: TRUE
    paho:
      short: paho
      settings:
        USE_PAHO: TRUE

buildtype:
  default: release
  choices:
    debug:
      short: deb
      buildType: Debug
    release:
      short: rel
      buildType: Release
